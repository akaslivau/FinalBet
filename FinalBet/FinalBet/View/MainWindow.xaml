<Controls:MetroWindow x:Class="FinalBet.MainWindow"
                      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                      xmlns:svgc="http://sharpvectors.codeplex.com/svgc/"
                      xmlns:ext="clr-namespace:FinalBet.Extensions"
                      xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
                      xmlns:properties="clr-namespace:FinalBet.Properties"
                      xmlns:local="clr-namespace:FinalBet"
                      mc:Ignorable="d"
                      Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal">
            <Button Height="40" Width="100" Content="Test" Command="{Binding TestCommand}"/>
        </StackPanel>
            <TabControl Grid.Row="1">
                <TabControl.Resources>
                    <Style TargetType="{x:Type TextBlock}">
                        <Setter Property="Margin" Value="{StaticResource ControlMargin}"/>
                    </Style>
                    <Style TargetType="{x:Type TextBox}">
                        <Setter Property="Margin" Value="{StaticResource ControlMargin}"/>
                    </Style>
                </TabControl.Resources>
                <TabItem Header="База данных"
                         DataContext="{Binding Database}"
                         >
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="3*"/>
                            <ColumnDefinition Width="10*"/>
                        </Grid.ColumnDefinitions>
                        <GroupBox Grid.Column="0" Header="Страны" 
                                  Margin="{StaticResource ControlMargin}">
                            <ListBox Grid.Column="0"
                                     ItemsSource="{Binding Items}"
                                     SelectedItem="{Binding Selected}"
                                     DisplayMemberPath="name"
                            />
                        </GroupBox>
                    <GroupBox Grid.Column="1" Header="Операции" Margin="{StaticResource ControlMargin}">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>
                                     <Grid Grid.Row="0">
                                         <Grid.ColumnDefinitions>
                                             <ColumnDefinition Width="Auto"/>
                                             <ColumnDefinition Width="Auto"/>
                                             <ColumnDefinition Width="*"/>
                                         </Grid.ColumnDefinitions>
                                             <TextBlock Text="{Binding Path=Selected.name, Mode=OneWay}" 
                                                        Style="{StaticResource Textblock.H1}"
                                                        Grid.Column="0"/>
                                            <Border BorderThickness="0.5" 
                                                    BorderBrush="LightSlateGray" 
                                                    Margin="{StaticResource ControlMargin}"
                                                    Grid.Column="1">
                                             <svgc:SvgViewbox 
                                                 Height="30" Width="40" 
                                                 Stretch="Uniform" 
                                                 ext:SvgViewboxAttachedProperties.Source="{Binding Path=FlagPath}"/>
                                             </Border>
                                             <StackPanel Orientation="Horizontal" Grid.Column="2">
                                                 <Button Width="150" Content="Test" Command="{Binding TestCommand}"/>
                                                 <Button Width="150" Content="Загрузить ссылки" Command="{Binding LoadUrlsCommand}"/>
                                                 <Button Width="150" Content="Загрузить матчи" Command="{Binding LoadMatchesCommand}"/>
                                             </StackPanel>
                            </Grid>
                                     <Border Grid.Row="1" BorderThickness="1" BorderBrush="LightSlateGray"/>
                            <DataGrid Grid.Row="2" 
                                    DataContext="{Binding LeagueUrls}"
                                    Margin="{StaticResource ControlMargin}"
                                    AutoGenerateColumns="False"
                                    CanUserDeleteRows="False"
                                    CanUserAddRows="False"
                                    HeadersVisibility="All"
                                    ItemsSource="{Binding Path=Items}"
                                    SelectedItem="{Binding Selected, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                    RowHeaderWidth="0"
                                    SelectionUnit="FullRow">
                                <DataGrid.Columns>
                                        <DataGridTextColumn Header="Год" Width="100"
                                                            IsReadOnly="True"
                                                            Binding="{Binding Path=year, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"
                                                            />
                                        <DataGridTextColumn Header="Название" Width="100"
                                                            IsReadOnly="True"
                                                            Binding="{Binding Path=name, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"
                                        />
                                </DataGrid.Columns>
                                <DataGrid.Style>
                                    <Style BasedOn="{StaticResource MetroDataGrid}" TargetType="{x:Type DataGrid}">
                                        <Setter Property="AlternatingRowBackground" Value="{DynamicResource GrayBrush10}" />
                                    </Style>
                                </DataGrid.Style>
                            </DataGrid>
                        </Grid>
                        </GroupBox>
                    </Grid>

            </TabItem>
                </TabControl>
    </Grid>
</Controls:MetroWindow>
